// DashboardView is intentionally not used as the main tab —
// the Workout tab serves as the home screen, matching Hevy's layout.
// This file is reserved for a future analytics/dashboard screen.
import SwiftUI

struct DashboardView: View {
    var body: some View {
        Text("Dashboard")
    }
}
